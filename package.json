{
  "name": "readme-cli-help",
  "version": "0.1.1",
  "bin": {
    "readme-cli-help": "./index.ts"
  },
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "lint": "npx @biomejs/biome check .",
    "format": "npx @biomejs/biome format --write ."
  }
}
